# Assessment: source-cluster robustness fixture (valid)

Verdict for c001 is strongly_supported, but this assessment notes the
cluster dependency on the official cluster. Knockout testing reports a
fragility score of 0.78, so the verdict is recorded as fragile despite
its strong rating.
